Foundation models are large-scale, general-purpose models trained on a massive amount of data. 

They can be adapted to a wide range of downstream tasks. In contrast, traditional machine learning models are often designed for specific tasks and trained on smaller, more focused datasets.